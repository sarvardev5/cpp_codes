#include <iostream>
#include <cmath>
using namespace std;

int main()
{

	double x = 3.99, y = 4, z;

	// z = max(x, y);
	// z = min(x, y);
	// z = pow(5, 3);
	// z = sqrt(9);
	// z = abs(-3);
	// z = round(x);
	// z = ceil(x);
	z = floor(x);

	cout << z;

	return 0;
}